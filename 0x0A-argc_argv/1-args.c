#include "main.h"

/**
 * main - program starts here
 * @argc: argument count to the main function
 * @argv: pointer to the command line inputs
 * Return: 0
 */
int main(int argc, __attribute__((unused)) char *argv[])
{
	printf("%d\n", argc - 1);

	return (0);
}
