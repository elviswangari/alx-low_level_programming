#include "main.h"

/**
* main - program starts here
* @argc: argument count to the function
* @argv: pointer to the command line command
* Return: 0
*/
int main(__attribute__((unused)) int argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
